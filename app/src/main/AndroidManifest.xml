<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.izrael.coronaindonesia">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:debuggable="false"
        android:icon="@drawable/icic"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logocovid"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LaporanActivity"></activity>
        <activity android:name=".EditActivty" />
        <activity android:name=".MenuKotlin" />
        <activity
            android:name=".MenuActivity"
            android:enabled="true"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".Splashscreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".thanks"
            android:noHistory="true" />
        <activity android:name=".verifcation" />
        <activity
            android:name=".Test_covid"
            android:noHistory="true" />
        <activity android:name=".Registrasi" />
        <activity
            android:name=".Test_covid19"
            android:noHistory="true" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".MapsActivity" />
        <activity
            android:name=".HomeActivity"
            android:noHistory="true" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>